// step 1 start

// step 2. Get knowledege of my input: numand result

// step 3. declare num and result

// step 4. take input for num from user

// step 5 num / 2 ---> result

// step 6. print out result

// step 7. End the program


const divideNum = (num) => {
  return (result = num / 2);
};

const printResult = (result) => {
  console.log(result);
};

printResult(divideNum(100));