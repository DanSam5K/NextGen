// step 1 start

// step 2. Get knowledege of my input which are firstname and lastname

// step 3. declare firstname lastname and result

// step 4. take input for firstname and lastname from user

// step 5 firstname + lastname -> result

// step 6. print out result

// step 7. End the program


const fullName = (firstName, lastName) => {
    return (result = firstName + lastName);
  };
  
  const printFullName = (result) => {
    console.log(result);
  };
  
  printFullName(fullName('Jacinta', ' Okonkwo'));