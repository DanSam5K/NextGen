// step 1 start

// step 2. Get knowledege of my input: firstname,  and result

// step 3. declare firstname, lastname and result

// step 4. take input for firstname, lastname from user

// step 5 firstname + lastname---> result

// step 6. print out result

// step 7. End the program

const AddTwoName = (FirstName, LastName) => {
  return (result = FirstName + LastName);
};

const printResult = (result)=> {
  console.log(result);
};

printResult(AddTwoName(chukwu, benjamin));